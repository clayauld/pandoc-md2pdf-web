services:
  web:
    image: ghcr.io/clayauld/pandoc-md2pdf-web:latest
    platform: linux/amd64
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    restart: unless-stopped


