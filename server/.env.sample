# This file is an example. Copy it to .env and fill in the values.

# Controls how long conversion history and generated files are kept.
# After this time, the history and files will be deleted.
#
# Formats:
#   - hours: "1h", "24h"
#   - days: "1d", "7d"
#   - weeks: "1w", "4w"
#
# If not set, the default is 1 hour ("1h").
HISTORY_TTL="24h"
